# Copy to .env and adjust

# LLM provider: ollama | openai | nim
# For demo default is local Ollama — cost $0, shows self-hosted capability
LLM_PROVIDER=ollama

# Ollama (local)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_LLM_MODEL=qwen3.5:9b
OLLAMA_EMBED_MODEL=nomic-embed-text

# OpenAI (optional — used by cost-comparison middleware)
# Leave empty if you don't want API fallback
OPENAI_API_KEY=
OPENAI_LLM_MODEL=gpt-4o-mini
OPENAI_EMBED_MODEL=text-embedding-3-small

# NIM (optional — hosted NVIDIA inference)
NIM_API_KEY=
NIM_BASE_URL=https://integrate.api.nvidia.com/v1

# Qdrant
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=properties
QDRANT_EF=64
QDRANT_M=16

# Redis (cache)
REDIS_URL=redis://localhost:6379/0
CACHE_TTL_SECONDS=86400

# App
APP_ENV=local
LOG_LEVEL=INFO
LOG_JSON=true

# Cost — pricing in USD per 1K tokens, for cost-comparison only (Ollama local = $0)
OPENAI_GPT4O_MINI_INPUT_PER_1K=0.00015
OPENAI_GPT4O_MINI_OUTPUT_PER_1K=0.0006
OPENAI_EMBED_SMALL_PER_1K=0.00002
